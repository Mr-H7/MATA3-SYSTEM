import Link from "next/link"; import {requireOwnerPage} from "@/lib/page-auth"; import {prisma} from "@/lib/prisma"; export const dynamic="force-dynamic"; export default async function Markets(){await requireOwnerPage();const rows=await prisma.market.findMany();return <main className="p-8"><Link className="gold" href="/">← Dashboard</Link><h1 className="text-3xl font-bold my-5">Markets</h1>{rows.map((market)=><div className="bg-white border rounded p-4 mb-3" key={market.id}><b>{market.code}</b> · {market.name} · {market.currency}</div>)}</main>}
