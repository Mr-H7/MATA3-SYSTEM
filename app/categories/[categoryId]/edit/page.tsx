import Link from "next/link"; import {notFound} from "next/navigation"; import CategoryForm from "../../category-form"; import {prisma} from "@/lib/prisma"; export const dynamic="force-dynamic";
export default async function EditCategory({params}:{params:Promise<{categoryId:string}>}){const {categoryId}=await params;const [category,departments,categories]=await Promise.all([prisma.category.findUnique({where:{id:categoryId}}),prisma.department.findMany({orderBy:{name:"asc"}}),prisma.category.findMany({where:{archived:false},select:{id:true,name:true,departmentId:true}})]);if(!category)notFound();return <main className="p-8 max-w-2xl"><Link className="gold" href="/categories">← Categories</Link><h1 className="text-3xl font-bold my-5">Edit Category</h1><CategoryForm departments={departments} categories={categories} initial={{id:category.id,name:category.name,departmentId:category.departmentId,parentId:category.parentId||"",archived:category.archived}}/></main>}
