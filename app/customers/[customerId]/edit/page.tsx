import Link from "next/link"; import {notFound} from "next/navigation"; import CustomerForm from "../../customer-form"; import {canAccessMarket,requireUser} from "@/lib/auth"; import {prisma} from "@/lib/prisma"; export const dynamic="force-dynamic";
export default async function EditCustomer({params}:{params:Promise<{customerId:string}>}){const user=await requireUser();const {customerId}=await params;const customer=await prisma.customer.findUnique({where:{id:customerId},include:{market:true}});if(!customer||!canAccessMarket(user,customer.market.code))notFound();return <main className="p-8 max-w-2xl"><Link className="gold" href="/customers">← Customers</Link><h1 className="text-3xl font-bold my-5">Edit Customer</h1><CustomerForm initial={{id:customer.id,marketCode:customer.market.code,name:customer.name,phone:customer.phone,email:customer.email||"",city:customer.city||"",address:customer.address||"",notes:customer.notes||""}}/></main>}
