import {NextResponse} from "next/server";
import {canAccessMarket,canManageGlobalData,requireOwner,requireUser} from "@/lib/auth";
import {prisma} from "@/lib/prisma";
export async function GET(_request:Request,{params}:{params:Promise<{id:string}>}){try{const user=await requireUser();if(user.role!=="OWNER")return NextResponse.json({error:"You do not have permission to perform this action"},{status:403});const {id}=await params;const row=await prisma.supplier.findUniqueOrThrow({where:{id},include:{market:true}});if(row.market?!canAccessMarket(user,row.market.code):!canManageGlobalData(user.role))return NextResponse.json({error:"Market access denied"},{status:403});return NextResponse.json(row)}catch(error){return NextResponse.json({error:error instanceof Error?error.message:"Not found"},{status:404})}}
export async function PATCH(request:Request,{params}:{params:Promise<{id:string}>}){try{const user=await requireUser();if(user.role!=="OWNER")return NextResponse.json({error:"You do not have permission to perform this action"},{status:403});const {id}=await params;const body=await request.json();const existing=await prisma.supplier.findUniqueOrThrow({where:{id},include:{market:true}});if(existing.market?!canAccessMarket(user,existing.market.code):!canManageGlobalData(user.role))return NextResponse.json({error:"Market access denied"},{status:403});let marketId:null|string=null;if(body.marketCode){const market=await prisma.market.findUniqueOrThrow({where:{code:body.marketCode}});if(!canAccessMarket(user,market.code))return NextResponse.json({error:"Market access denied"},{status:403});marketId=market.id}else if(!canManageGlobalData(user.role))return NextResponse.json({error:"A market is required for market managers"},{status:403});if(!body.name?.trim())return NextResponse.json({error:"Name is required"},{status:400});return NextResponse.json(await prisma.supplier.update({where:{id},data:{name:body.name.trim(),contactPerson:body.contactPerson||null,phone:body.phone||null,whatsapp:body.whatsapp||null,marketId,location:body.location||null,policy:body.policy||null,moq:body.moq||null,shippingAvailable:Boolean(body.shippingAvailable),notes:body.notes||null}}))}catch(error){const message=error instanceof Error?error.message:"Update failed";return NextResponse.json({error:message==="FORBIDDEN"?"You do not have permission to perform this action":message},{status:message==="FORBIDDEN"?403:400})}}
