import Link from"next/link";import{requireOwnerPage}from"@/lib/page-auth";import{prisma}from"@/lib/prisma";import ReturnForm from"../return-form";export default async function NewReturn(){await requireOwnerPage();const sales=await prisma.sale.findMany({where:{status:{not:"CANCELLED"}},include:{market:true,items:true},orderBy:{createdAt:"desc"},take:100});return <main className="p-8 max-w-3xl"><Link href="/returns" className="gold">← Returns</Link><h1 className="text-3xl font-bold my-5">New Return</h1><ReturnForm sales={JSON.parse(JSON.stringify(sales))}/></main>}
