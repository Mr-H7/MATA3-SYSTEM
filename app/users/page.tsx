import Link from"next/link";import{requireOwnerPage}from"@/lib/page-auth";import{prisma}from"@/lib/prisma";export const dynamic="force-dynamic";export default async function Users(){await requireOwnerPage();const rows=await prisma.user.findMany({select:{id:true,username:true,name:true,role:true,marketScope:true,active:true},orderBy:{name:"asc"}});return <main className="p-8"><Link href="/" className="gold">← Dashboard</Link><div className="flex justify-between"><h1 className="text-3xl font-bold my-5">Users</h1><Link href="/users/new" className="bg-[#c6a227] rounded px-4 py-3">+ User</Link></div>{rows.map(u=><div className="bg-white border rounded p-4 mb-2 flex justify-between" key={u.id}><span>{u.name} · {u.username} · {u.role} · {u.marketScope} · {u.active?"Active":"Disabled"}</span><Link className="underline" href={`/users/${u.id}/edit`}>Edit</Link></div>)}</main>}
