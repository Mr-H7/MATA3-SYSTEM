import Link from"next/link";import{notFound}from"next/navigation";import{requireOwnerPage}from"@/lib/page-auth";import{prisma}from"@/lib/prisma";import UserForm from"../../user-form";export default async function EditUser({params}:{params:Promise<{id:string}>}){await requireOwnerPage();const{id}=await params;const u=await prisma.user.findUnique({where:{id},select:{id:true,username:true,name:true,email:true,role:true,marketScope:true,active:true}});if(!u)notFound();return <main className="p-8 max-w-2xl"><Link href="/users" className="gold">← Users</Link><h1 className="text-3xl font-bold my-5">Edit User</h1><UserForm initial={{...u,username:u.username||"",password:""}}/></main>}
